[pytest]
markers =
    ; To run all tests run without any markers
    security: Tests that test the security features
    wip: Tests that are not yet complete
    broken: Tests to be fixed
filterwarnings =
    ignore::DeprecationWarning
